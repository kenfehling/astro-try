import React from 'react'
import { styled } from '@linaria/react'

const NavContainer = styled.div`

`;

const Nav = () => (
    <NavContainer>
        Nav
    </NavContainer>
);

export default Nav;
